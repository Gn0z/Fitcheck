# FitCheck 版本日志

本文档记录 FitCheck 每个版本的重要更新。

版本号采用 `主版本.次版本.修订版本` 格式：

- 主版本：存在不兼容的数据结构或使用方式变更。
- 次版本：新增向后兼容的功能。
- 修订版本：修复问题或进行小幅体验优化。

## [未发布]

## [1.4.0] - 2026-07-23

### 新增

- 支持在“安排训练”页面直接删除当前计划的已安排日期；删除排期不会影响既有训练完成记录。

### 改进

- 将训练徽章系统调整为 14 项二维训练成就，使用圆角方形底板和内联 SVG 图形，移除 3D 厚度、翻转交互与运行时位图依赖。
- 为四组成就加入“初燃”“落印”“开锋”“涓流”等独立名称，并以同一绿色由浅至深呈现等级进阶；保留原有解锁条件、解锁记录、锁定状态和备份兼容。
- 更新解锁与详情弹窗，使名称、条件、等级和解锁日期无需翻转即可直接查看。
- 删除二维成就系统不再使用的旧 3D 徽章样式，避免继续维护两套视觉实现。
- 为备份恢复增加计划、动作、排期、完成记录、成就和昵称的深度校验，限制文件及集合规模，并拒绝重复 ID、无效引用、越界字段和恶意 HTML。
- 将本地持久化迁移为单一原子状态快照；导入写入失败时保持原数据不变，异常本地数据会进入可恢复的安全启动界面，同时继续兼容现有 v1 与缺少成就或昵称字段的旧备份。
- 更新 Service Worker 应用外壳缓存至 `fitcheck-shell-v28`，加入数据校验模块，并为样式和完整 JavaScript 模块图统一使用 `v28` 资源版本标识。

### 修复

- 修复训练完成后的历史记录：成功页替换训练中页面，刷新或前进后退遇到无有效会话的训练路由时自动回到训练详情或首页。

## [1.3.0] - 2026-07-22

### 新增

- 加入连续训练、累计打卡、完成次数和累计时长四类共 14 枚徽章，并在“我的”页面展示全部徽章及解锁状态。
- 完成训练获得新徽章时，以可翻转的 3D 徽章弹窗逐枚展示，背面刻有解锁日期。
- 自动根据已有训练记录静默补发符合条件的徽章；已解锁徽章不会重复评估或撤销。
- 备份导入与导出包含徽章解锁记录，同时保持旧版备份兼容。
- “我的徽章”默认展示等级最高的 3 枚已解锁徽章，同等级优先最近解锁；不足 3 枚时以高等级未解锁徽章补齐，并支持展开全部徽章。
- 支持点击收藏页中的任意徽章打开详情弹窗，放大查看徽章并翻转至背面。
- 将徽章重构为具有前后景深和多层深灰侧边的立体硬币，翻转时可看到真实厚度、移动高光与独立投影。
- 重新绘制连续训练、累计打卡、完成次数和累计时长四类矢量图形，统一校准视觉中心并以石墨面、金属斜面和压印阴影增强浮雕质感。
- 重新绘制大师级金色月桂资源，以饱满叶片、自然枝条、叶脉高光和暗金斜面替代原有简化线条；月桂仅用于第四级黑金大师徽章。
- 徽章背面增加压刻同心环、分隔线和对应等级名称，使一至四级翻转后的信息结构保持一致。
- 在训练计划列表、训练详情和“安排训练”页面展示对应计划已经安排的全部训练日期。
- 增加与现有黑白视觉一致的应用开屏页，在应用初始化期间展示原图标、品牌名称和简短说明，初始化完成后立即淡出。
- “我的”页面支持点击昵称进行修改，昵称保存在当前设备并随备份文件导入导出；旧备份恢复时使用默认昵称“训练者”。
- 新增“关于 FitCheck”页面，展示当前 1.3.0 版本号和本次更新内容。

### 改进

- 首页连续训练提示会显示距离下一枚连续训练徽章所需的天数。
- 移除应用各主页面 FitCheck 品牌文字右下角的装饰圆点，使品牌标题更加简洁。
- 移除“我的”页面中尚无实际功能的“个人资料”和“训练目标”入口。
- 将累计训练时长改为数字与“累计分钟”分行展示，并对超长数字缩小字号，避免窄屏胶囊出现单位挤压。
- 精简“我的”页面昵称下方的打卡摘要，移除重复的“数据保存在本机”说明。
- 更新 Service Worker 应用外壳缓存至 `fitcheck-shell-v22`，加入四类图标和大师级月桂矢量资源，并确保最新徽章视觉、训练日期展示、开屏页及“我的”页面更新可以离线使用。
- 为样式和完整 JavaScript 模块图增加 `v22` 资源版本标识，避免已安装 PWA 更新时混用新 HTML 与旧缓存资源。
- Service Worker 安装新版本时强制重新获取应用外壳资源，避免旧缓存造成 JavaScript 模块版本混用。

## [1.2.1] - 2026-07-14

### 改进

- 将原本内嵌在 HTML 中的样式与脚本分别迁移到独立的 `css/styles.css` 和 `js/app.js` 文件，降低单文件复杂度。
- 将 JavaScript 按职责拆分为应用入口、状态存储、训练流程、训练记录与通用工具模块，并使用 ES Module 组织依赖关系。
- 封装训练计划、日期排期和完成记录的共享状态；读取操作返回数据副本，写入统一通过受控接口完成并持久化到 `localStorage`。
- 更新 Service Worker 应用外壳缓存至 `fitcheck-shell-v13`，加入拆分后的 CSS 和 JavaScript 模块，保持 PWA 离线可用。

### 文档

- 新增项目级 `AGENTS.md`，统一代码组织、状态管理、数据兼容、PWA、验证和变更记录规范。

## [1.2.0] - 2026-07-13

### 新增

- 支持为同一个训练计划一次选择多个非连续日期，并自动跳过已有排期。
- 支持同一训练计划在一天内多次打卡，每次分别记录并累计课程数与训练时间。

### 改进

- 使用 `requestAnimationFrame` 平滑更新训练和休息倒计时圆环。
- 倒计时数值与圆环统一基于绝对结束时间计算，并在应用从后台恢复时立即校正。
- 将训练记录页的占位图形替换为最近十四周训练热力图，以高辨识度灰阶表示每日累计训练分钟数，每 8 分钟提升一级强度。

### 修复

- 修复首页顶部“查看计划”按钮被错误识别为返回按钮而无法打开计划列表的问题。
- 将“我的”页统计标签由“完成课程”调整为“完成训练”，使含义与实际统计逻辑一致。

## [1.1.0] - 2026-07-13

### 改进

- 重构页面滚动策略：由各页面内容区独立滚动，训练中和完成页保持固定布局。
- 二级页面的返回栏与标题支持吸顶，并适配 iPhone 安全区。
- 使用 History API 维护单页应用导航状态，支持浏览器和系统返回恢复页面上下文。
- 主导航页面改为平级替换，不再互相产生返回记录；在主页面触发浏览器返回时保持当前页面。
- 主导航页面仅允许纵向触摸滚动，并拦截横向拖动，避免返回手势造成页面跟随滑动。
- 增加应用内左缘滑动返回，并对距离、方向和速度设置防误触阈值。
- 编辑内容未保存或训练仍在进行时，离开前增加确认保护。

## [1.0.1] - 2026-07-10

### 修复

- 修复 iOS Safari 中训练日期输入框因原生控件最小宽度而溢出卡片的问题。

### 改进

- 采用方案 D 重新设计训练日期控件：突出显示月日，并补充年份、星期、日历图标和进入提示。
- 保留 iOS 原生日期选择器交互，同时使用稳定的自定义视觉层统一手机端对齐效果。
- 更新离线缓存版本，使已安装的 PWA 能获取本次界面修复。

## [1.0.0] - 2026-07-10

FitCheck 首个可用版本。完成了从高保真原型到可安装、可离线运行的个人训练 PWA。

### 新增

- 黑、白、灰配色的移动端 App 界面，以及首页、训练计划、训练记录和个人中心导航。
- 本地训练计划管理，可添加、编辑和删除训练计划。
- 动作编辑功能，可设置动作名称、组数、计量方式、次数或时长、休息秒数和动作备注。
- 日期排期功能，可将训练计划安排到指定日期，并允许同一天安排多个不同计划。
- 今日训练列表，可展示当天计划及完成状态。
- 可实际执行的训练流程，按照计划中的动作和组数依次推进。
- 时长动作的数字倒计时、圆环进度、暂停、继续和提前完成功能。
- 次数动作的目标次数展示和手动完成功能。
- 最后一个动作完成后自动生成打卡记录。
- 训练记录、连续打卡天数、累计打卡天数和累计训练分钟数统计。
- JSON 数据备份导出与导入恢复功能。
- 使用 `localStorage` 在当前设备保存训练计划、日期安排和完成记录。
- PWA 安装支持，包括应用图标、Web App Manifest 和独立窗口显示。
- Service Worker 离线缓存，可在首次联网加载后离线打开应用。
- GitHub Pages 免费 HTTPS 部署，可在 iPhone Safari 中添加到主屏幕。

### 改进

- 调整首页训练卡片的左侧强调线，使其与卡片内容区域协调对齐。
- 统一训练计划表单中并排输入框、下拉框和动态字段的高度与基线。
- 针对手机和桌面浏览器完善响应式布局。

### 当前限制

- 一个训练计划每次只能安排一个日期，尚不能批量选择多天。
- 同一计划同一天只记录一次完成，不累计当天的重复训练。
- 连续打卡提示中的徽章尚未形成完整的徽章系统。
- 圆环倒计时仍采用较低频率更新，动画流畅度有待提升。
- 所有用户数据仅保存在当前浏览器或已安装的 PWA 中，不会自动跨设备同步。

后续功能规划详见 [ROADMAP.md](./ROADMAP.md)。
