# FitCheck 版本日志

本文档记录 FitCheck 每个版本的重要更新。

版本号采用 `主版本.次版本.修订版本` 格式：

- 主版本：存在不兼容的数据结构或使用方式变更。
- 次版本：新增向后兼容的功能。
- 修订版本：修复问题或进行小幅体验优化。

## [未发布]

### 计划新增

- 支持为同一个训练计划一次选择多个非连续日期。
- 加入徽章系统和本地解锁记录。
- 支持同一训练计划在一天内多次打卡，并累计课程数与训练时间。

### 计划改进

- 使用基于动画帧的进度更新，提升圆环倒计时的流畅度。
- 保持倒计时数值、圆环进度与后台时间校正一致。

### 改进

- 重构页面滚动策略：由各页面内容区独立滚动，训练中和完成页保持固定布局。
- 二级页面的返回栏与标题支持吸顶，并适配 iPhone 安全区。
- 使用 History API 维护单页应用导航状态，支持浏览器和系统返回恢复页面上下文。
- 增加应用内左缘滑动返回，并对距离、方向和速度设置防误触阈值。
- 编辑内容未保存或训练仍在进行时，离开前增加确认保护。

## [1.0.1] - 2026-07-10

### 修复

- 修复 iOS Safari 中训练日期输入框因原生控件最小宽度而溢出卡片的问题。

### 改进

- 采用方案 D 重新设计训练日期控件：突出显示月日，并补充年份、星期、日历图标和进入提示。
- 保留 iOS 原生日期选择器交互，同时使用稳定的自定义视觉层统一手机端对齐效果。
- 更新离线缓存版本，使已安装的 PWA 能获取本次界面修复。

## [1.0.0] - 2026-07-10

FitCheck 首个可用版本。完成了从高保真原型到可安装、可离线运行的个人训练 PWA。

### 新增

- 黑、白、灰配色的移动端 App 界面，以及首页、训练计划、训练记录和个人中心导航。
- 本地训练计划管理，可添加、编辑和删除训练计划。
- 动作编辑功能，可设置动作名称、组数、计量方式、次数或时长、休息秒数和动作备注。
- 日期排期功能，可将训练计划安排到指定日期，并允许同一天安排多个不同计划。
- 今日训练列表，可展示当天计划及完成状态。
- 可实际执行的训练流程，按照计划中的动作和组数依次推进。
- 时长动作的数字倒计时、圆环进度、暂停、继续和提前完成功能。
- 次数动作的目标次数展示和手动完成功能。
- 最后一个动作完成后自动生成打卡记录。
- 训练记录、连续打卡天数、累计打卡天数和累计训练分钟数统计。
- JSON 数据备份导出与导入恢复功能。
- 使用 `localStorage` 在当前设备保存训练计划、日期安排和完成记录。
- PWA 安装支持，包括应用图标、Web App Manifest 和独立窗口显示。
- Service Worker 离线缓存，可在首次联网加载后离线打开应用。
- GitHub Pages 免费 HTTPS 部署，可在 iPhone Safari 中添加到主屏幕。

### 改进

- 调整首页训练卡片的左侧强调线，使其与卡片内容区域协调对齐。
- 统一训练计划表单中并排输入框、下拉框和动态字段的高度与基线。
- 针对手机和桌面浏览器完善响应式布局。

### 当前限制

- 一个训练计划每次只能安排一个日期，尚不能批量选择多天。
- 同一计划同一天只记录一次完成，不累计当天的重复训练。
- 连续打卡提示中的徽章尚未形成完整的徽章系统。
- 圆环倒计时仍采用较低频率更新，动画流畅度有待提升。
- 所有用户数据仅保存在当前浏览器或已安装的 PWA 中，不会自动跨设备同步。

后续功能规划详见 [ROADMAP.md](./ROADMAP.md)。
