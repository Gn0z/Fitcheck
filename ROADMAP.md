# FitCheck 后续版本计划

## 排期与激励

- 支持为同一个训练计划一次选择多个非连续日期。
- 设计徽章系统，包括解锁条件、徽章列表、解锁状态和本地持久化。

## 打卡与统计

- 支持同一训练计划在同一天多次完成并分别写入打卡记录。
- 多次完成需要累计训练分钟数和完成课程数。
- 同一天无论完成多少次，连续打卡和累计打卡天数仍只增加一天。

## 训练体验

- 优化圆环倒计时渲染，使用基于动画帧的平滑进度更新，减少当前低帧率和跳动感。
- 保持倒计时数值、圆环进度与后台时间校正一致。
