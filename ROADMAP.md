# FitCheck 后续版本计划

## 排期与激励

- 支持为同一个训练计划一次选择多个非连续日期。
- 设计徽章系统，包括解锁条件、徽章列表、解锁状态和本地持久化。

## 打卡与统计

- 支持同一训练计划在同一天多次完成并分别写入打卡记录。
- 多次完成需要累计训练分钟数和完成课程数。
- 同一天无论完成多少次，连续打卡和累计打卡天数仍只增加一天。

## 训练体验

- 优化圆环倒计时渲染，使用基于动画帧的平滑进度更新，减少当前低帧率和跳动感。
- 保持倒计时数值、圆环进度与后台时间校正一致。

## 界面滚动与导航

- 重构页面滚动策略，不再让所有页面统一产生纵向滚动。
- 对训练中、训练完成等内容固定且较短的页面采用整页固定布局；内容可能增长的首页、计划列表、记录和编辑页面保留内容区滚动。
- 将二级页面的返回栏和页面标题设计为吸顶区域，并正确适配 iPhone 安全区；滚动时仅移动下方内容。
- 避免页面主体与内部内容区同时滚动，减少 iOS 回弹、位置跳动和底部导航遮挡。
- 为当前单页应用建立统一导航栈，页面切换时使用浏览器 History API 写入和恢复页面状态。
- 支持 Safari 返回按钮、系统返回操作以及可用情况下的屏幕左缘返回手势，并同步恢复计划详情、编辑页和日期安排页的上下文。
- 针对主屏幕 PWA 中系统手势不可用或表现不一致的情况，增加应用内左缘滑动返回兜底，并设置距离、方向和速度阈值以避免与纵向滚动冲突。
- 编辑表单存在未保存内容时，返回前给出确认；训练进行中禁用直接滑退或要求确认，避免意外丢失进度。
