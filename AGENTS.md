# FitCheck 项目开发规范

## 适用范围

- 本文件适用于整个 FitCheck 仓库。
- 后续新增子目录时，只有确实需要更具体的规则，才在子目录中增加 `AGENTS.md`；不要重复本文件已有内容。
- 开始修改前先阅读相关代码、现有文档和工作区状态。存在会实质影响实现方案的不确定信息时，先向用户确认，不要自行猜测。

## 项目定位

- FitCheck 是基于原生 HTML、CSS 和 JavaScript ES Module 的移动端 PWA。
- 在没有明确需求的情况下，不引入前端框架、构建系统或大型运行时依赖。
- 优先保持项目可直接通过静态服务器运行、易于部署、可离线使用。
- 重构应以保持现有行为和本地数据兼容为前提，避免将结构调整与无关功能改动混在一起。

## 代码组织

- HTML 负责页面结构和语义，不在 HTML 中重新堆积大段 CSS 或 JavaScript。
- CSS 放在 `css/` 目录，JavaScript 放在 `js/` 目录，并通过外部资源引用。
- JavaScript 按职责划分模块：应用入口负责初始化和协调，数据层负责状态与持久化，业务模块负责各自流程，工具模块只提供通用且尽量纯粹的能力。
- 新代码优先加入职责匹配的现有模块；当一个职责已经独立、会持续扩展或可单独验证时，再创建新模块。
- 不要仅为了缩短文件而拆分模块。每个模块应具有清晰边界、有限公开接口和明确依赖方向。
- 避免循环依赖。底层工具和数据模块不得反向依赖应用入口或具体页面流程。
- `app.js` 应保持为应用编排层，不重新演变为容纳全部业务实现的单体文件。

## 状态与数据

- 共享业务状态必须由统一的数据层管理，其他模块通过公开接口读取或修改。
- 不在页面、渲染或业务流程模块中直接读写 `localStorage`；持久化细节集中在数据层。
- 不向外暴露可被任意修改的内部状态引用。读取结果应采用副本、只读数据或明确不可变的返回约定。
- 状态修改应使用语义明确的操作接口，避免跨模块直接替换数组、对象或内部变量。
- 业务计算应尽量写成无副作用的函数：输入通过参数传入，结果通过返回值产生，不隐式修改共享数据。
- 修改持久化数据结构时，必须同时评估旧数据、备份导入、导出格式、校验规则和迁移方案。
- 除非明确决定进行不兼容升级，否则已有本地数据和历史备份应继续可用。

## HTML 与可访问性

- 使用语义化 HTML，交互元素应使用正确的 `button`、`input`、`label` 等元素，不用普通容器模拟控件。
- 新增交互时同步考虑键盘操作、焦点、可读标签、状态提示和读屏体验。
- 用户生成或导入的数据进入 HTML 前必须经过安全处理，不直接拼接未经转义的文本。
- 保持移动端布局、触摸交互、iPhone 安全区和不同视口尺寸下的可用性。

## CSS

- 优先复用现有 CSS 变量、组件类和布局模式，避免重复定义近似样式。
- 通用设计值放入变量；页面或组件特有样式应保持清晰作用域，避免过度宽泛的选择器。
- 除确实由运行时数据驱动的样式外，不新增内联样式。
- 修改公共样式时检查其对所有页面和状态的影响。
- 只有当样式职责形成稳定边界或单文件已明显阻碍维护时，才继续拆分 CSS 文件。

## JavaScript

- 使用 ES Module 的显式 `import` 和 `export`；只导出其他模块实际需要的接口。
- 使用 `const` 作为默认声明方式，只有变量确实需要重新赋值时才使用 `let`，不使用 `var`。
- 函数和变量采用能表达业务含义的名称，避免无意义缩写和含糊的通用名称。
- DOM 查询、状态修改、持久化和纯计算应尽量分层，避免在一个函数中混合过多职责。
- 事件入口只负责识别意图和调用对应能力，复杂流程应放入所属模块。
- 异步操作必须处理失败状态，并向用户提供可理解的反馈；不要静默吞掉会影响数据或核心流程的异常。
- 不依赖未声明的全局变量，不通过全局对象在模块间传递业务状态。

## PWA 与静态资源

- 新增、删除或重命名运行所需的静态资源时，同步检查 `service-worker.js` 的应用外壳缓存清单。
- 修改会影响离线资源的内容或路径时，升级缓存名称，确保已安装版本能够获取新资源。
- Service Worker 的修改必须同时验证在线首次加载、缓存命中和离线回退路径。
- 保持资源路径使用适合静态部署的相对路径。

## 安全与数据完整性

- 不在仓库中写入密码、令牌、个人隐私数据或仅适用于个人机器的秘密配置。
- 导入的外部数据必须先解析、校验，再写入状态；失败时不得破坏当前有效数据。
- 删除、覆盖或批量替换用户数据前，应提供明确提示并获得确认。
- 对数字、日期、标识符和数组结构进行边界校验，不依赖隐式类型转换掩盖异常数据。

## 变更与文档

- 保持改动聚焦于当前任务，不顺带重写无关代码。
- 工作区中已有但与当前任务无关的修改属于用户，必须保留，不覆盖、不回退。
- 用户可见的新增、改进、修复，以及重要的架构或数据结构调整，应写入 `CHANGELOG.md` 的“未发布”部分。
- 未来计划写入 `ROADMAP.md`，已经完成的内容应从计划中移除或转入版本日志。
- 注释主要解释设计原因、边界条件和不明显的约束，不重复描述代码表面行为。

## 验证要求

- 完成修改后，根据风险执行与改动范围相称的验证，不能只确认文件能够保存。
- JavaScript 变更至少执行所有模块的语法检查，并检查导入路径和导出名称。
- 所有修改至少执行 `git diff --check`，确认没有空白错误或冲突标记。
- 涉及页面或资源引用时，通过本地 HTTP 服务器验证 HTML、CSS、JavaScript 和其他静态资源能够正常加载；不要只通过 `file://` 判断 ES Module 是否可用。
- 涉及状态或持久化时，验证新增、读取、修改、删除、刷新恢复以及异常输入，不仅验证正常路径。
- 涉及导航、训练流程、导入导出或 PWA 时，回归与改动相邻的关键用户流程。
- 如果当前环境无法完成必要验证，必须明确说明未验证的内容和原因，不能声称已经通过。

## 完成标准

- 代码放置符合模块职责，且没有绕过公共接口修改共享状态。
- 现有用户数据和核心流程未被无意破坏。
- 必要的 PWA 缓存、文档和版本日志已经同步。
- 相关检查通过，临时脚本、调试输出和本地测试进程已清理。
- 最终说明应列出主要改动、验证结果和仍存在的限制。
